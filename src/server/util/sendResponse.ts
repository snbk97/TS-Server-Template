/* import { IServiceResponse } from "../../types/resopnse";

type ResponseType = IServiceResponse;

const sendResponse = (responseObject: ResponseType) => {
  const { success, message } = responseObject;
  const response: ResponseType = {
    success,
    message
  };
  return response;
};

export default sendResponse; */
